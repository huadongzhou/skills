# P0 改动记录 — 两处完成判据

- 依据：`writing-for-agents-审阅-汇总.md` 的 P0 清单
- 范围：P0 定义的三处判据（cognition ×2、foundation ×1）＋ 一处连带修复（cognition ×1）
- 落盘：行级替换，共 4 行；`git diff` 确认 `skills/` 下仅此两文件、仅此 4 行改动

---

## 1 · cognition · 第二步完成判据（`SKILL.md:70`）

**改前**
> **完成判据**：每个待答问题都有至少一条可追溯来源，或一条明确的「检索无果」。

**改后**
> **完成判据**：每个待答问题都留下一条可追溯的取证记录——已采用的来源，或标为「检索无果」时已尝试的检索式（≥2 条且彼此不同）与该问题的留白去向；两者皆无即为未完成。

**为什么**：原判据是一个无约束的析取项，「检索无果」四个字即可满足，demand 归零（框架 `Steps and completion criteria` — 判据须 both checkable and exhaustive）。改后析取项保留——检索确实可能无果——但**「无果」本身需要举证**：交出已尝试的检索式与留白去向。逃逸口没有被封死，只是变得要付出成本。

---

## 2 · cognition · 兜底原则（`SKILL.md:104`）

**改前**
> **兜底原则**：宁可留白，不可编造。某节点标注「检索无果」时，第三步的来源要求对该节点自动豁免，改以「已标注检索无果」为完成条件。

**改后**
> **兜底原则**：宁可留白，不可编造。某节点标「检索无果」时，第三步的要求降为：该节点只写「检索无果：<缺什么>」，**不产出反转**；反转只留给有来源支撑的节点。

**为什么**：这是整条逃逸链的最后一环。原句把无来源节点的第三步要求「自动豁免」，于是可以「先标无果 → 再写无来源的反转」，全篇的 exhaustiveness bar 就此塌陷。改后无来源的节点只留白、不产出反转——**反转只留给有来源支撑的节点**，与该技能「价值在反转」的定位一致：宁可少一个反转，不可多一个无据的反转。

**与审阅报告的一处偏离（有意为之）**：报告给出的替换文案开头是「留白优先于推测」，我保留了原文的「宁可留白，不可编造」。理由：该句是框架 `Leading words` 明确允许的硬护栏例外（「A prohibition earns its place only as a hard guardrail you cannot phrase positively; even then, pair it with the positive target」），报告自身在「不建议改的」一节也要求保留同类表述（`L108`「不编造」）。删掉它会以「消重」之名损失一条真护栏，而它的重复问题属 P2（单一事实源）范围。

---

## 3 · cognition · 第三步完成判据（`SKILL.md:81`）— 连带修复

**改前**
> **完成判据**：每个保留维度三件套齐全、反转齐备，且每条反转都指出它推翻的具体默认认知、附至少一个支撑来源。

**改后**
> **完成判据**：每个保留维度三件套齐全、反转齐备，且每条反转都指出它推翻的具体默认认知、附至少一个支撑来源；标「检索无果」的节点按「兜底原则」处理。

**为什么（这条不在原 P0 清单内）**：第 2 条改动使「检索无果」的节点不再要求「反转齐备」，而第三步判据仍无条件要求「每个保留维度……反转齐备」，两处直接冲突。原文本已有同型冲突（「三件套齐全」vs「来源要求豁免」），但改写让它变得更尖锐。补一句指针即可消解，成本一行。若不补，agent 在第三步读到「反转齐备」时无从得知例外存在，很可能选择编造——正是本次要堵的那条路。

---

## 4 · foundation · 「更新」完成判据（`SKILL.md:83`）

**改前**
> - 完成判据：所处理文档的全部章节处置有交代，缺一份即未完成。

**改后**
> - **完成判据**：本次点名的每份文档，其每个章节都有一条处置（补齐／修正／保留／无变化），并在汇报中按章节列出处置与依据；标「无变化」须写出与技能自带模板的逐条对照结果（如「模板 3 个挖空点均已填」），仅凭印象标注不算。漏一个章节即未完成。

**为什么**：原判据三处硬伤，逐条对应修复——

| 硬伤 | 修复 |
|---|---|
| 「所处理文档」未定域（四份？还是点名的那几份？） | 定域为「**本次点名的每份文档**」，与 `L124` 的点名规则对齐 |
| 「有交代」不可观察（是决定过还是汇报出来？） | 改为「**并在汇报中按章节列出处置与依据**」，处置与依据都成为可见产物 |
| 计量单位自相矛盾（前半数「章节」、后半数「份」） | 统一为「章节」，「漏一个**章节**即未完成」 |
| 最严重：「无变化」无举证义务，可一键满足 | 标「无变化」**须写出与技能自带模板的逐条对照结果**，仅凭印象标注不算 |

**术语选择**：使用「**技能自带模板**」而非报告建议的「基准模板」——前者是 `L19` 已经存在的原措辞（「技能自带的 `templates/<名>.md`」），后者是 P1 才要立的术语。P0 只用已定义的词，不引入未定义的新词，改动自洽。

**样板来源**：`L105`（同步判据）是本仓库内最强的一条——「每个模板没有的标题与表头，都有一条处置……漏一个即未完成」。本次改写即照它的强度做。

---

## 未做与遗留

- **未做 P1 / P2 / P3**：指针消歧（`description` 重写、「基准模板/仓库模板」术语）、单一事实源收拢、章节顺序调整、措辞级修剪，均未触碰。
- **未改动其它文件**：`git diff --stat` 仅 cognition 3 行 + foundation 1 行。
- **未写入 `skill-darwin-results.tsv`**：该文件是 darwin 八维评分法的日志（列含 `old_score`/`new_score`/`dimension`/`eval_mode`），本次改动没有 paired 评分，写入会污染该日志的语义。如需留痕，建议另起一行并明确标注框架来源。
- **未提交**：仓库当前有前一轮重构的大量暂存改动（`A`/`M`/`R`），本次改动未单独成 commit，以免把它们一并卷入。
- **验证状态：未验证**。框架对完成判据有明确要求——no-op 与判据强度须「settle it by running the document, not by debate」。P0 属于「sharpen the bound」这一本地且便宜的修复，其效果需以同一任务实跑一轮、对比「检索无果」与「无变化」的出现率来确认。
