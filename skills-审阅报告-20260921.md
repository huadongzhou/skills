# 子技能审阅报告 · 2026-09-21

**审阅对象**：`skills/cognition/`、`skills/foundation/`
**判据来源**：`writing-for-agents` / `SKILL-MECHANICS.md`（Matt Pocock 上游镜像，MIT）
**审阅方式**：三代理并行（各审一份 + 一份交叉审），主代理复核实证项
**结论一句话**：两份技能的**工程完备度显著超标**（完成判据、异常表、留白机制都写得比判据要求更硬），主要失分集中在**层级摆放（公共规则寄居分支）**、**同一含义多份实现**、**negation 驱动**三处。共 **6 项 P0**、**11 项 P1**、**9 项 P2**，其中 1 项 P0 存在**覆盖用户自有文件**的实际风险。

---

## 一、结论摘要

| 维度 | cognition | foundation |
|---|---|---|
| description 指针质量 | ⚠️ 正文身份重复、同义 branch 重复 | ⚠️ 身份重复、元信息填充 |
| 调用方式选择 | ✅ model-invoked 合理 | ✅ model-invoked 合理 |
| 信息层级 / 分支下推 | ⚠️ 跳过条件块属全流程却挂在第一步 | ❌ 对照口径、免确认词表两处公共规则寄居分支 |
| co-location | ⚠️ 留白规则散在 3 节 | ⚠️ 管辖范围散在 3 节、核验散在 4 节 |
| 完成判据 | ✅ 清晰且 demanding（四类取证穷尽） | ✅ 可核验（漏一章节即未完成） |
| leading words / negation | ❌ 跳过条件是 negation 驱动的伪解析器 | ⚠️ 写入禁令过载，多无正向目标 |
| pruning（SSOT / cache / no-op） | ⚠️ 「≥2 独立来源」5 处复述 | ⚠️ 管辖范围 3 处复述、基线口径 4 处复述 |
| sprawl | ⚠️ 130 行偏长 | ⚠️ 140 行 / 18KB 偏长 |

---

## 二、跨技能共性问题（优先修）

### 🔴 P0-1 · 免确认旁路有两份实现，且语义已漂移

**事实**：`cognition` L63–68 的跳过条件含**否决条件**（否定 / 延后 / 语气三类否决）；`foundation` L75 的免确认词表**只有正向枚举、无任何否决条件**。
**后果（真 bug）**：用户说「不要直接生成 AGENTS.md」，foundation 在枚举里命中「直接生成」→ **误判为免确认**，跳过 CHECKPOINT 直接落盘。cognition 对同一句话会正确判为不免确认。同一仓库里两个技能对同一句话给出相反处置。
**判据**：single source of truth（同一含义两份实现）+ 完成判据 clarity。
**处置建议**：统一为一份判定，foundation 侧补否决条件。

### 🔴 P0-2 · cognition 的「跳过条件」是一台字符偏移伪解析器

**事实**：L63–68 用「其后 6 字内」「前 4 字内」「后 8 字内」「后 2 字内」等字符距离规则，逐一枚举否定词、延后词、语气词的豁免与反豁免，共 5 行、约 400 字。
**判据**：三重违规——
1. **negation 驱动**：全段由「不要 / 别 / 先 / 等 / 吗 / 能不能」等被禁词构成，把被禁行为拉进上下文，正是判据点名的失败模式；
2. **no-op**：判断「用户是否真的授权我别问」是模型自带的语用能力，这 400 字相对默认行为是否改变行为存疑（判据要求：存疑时按默认处理，跑一遍再定，不要靠争论）；
3. **sprawl**：这 5 行挤在第一步完成判据之后，把该步骤最该被注意的判据挤到注意力边缘。
**改写建议**（正向表述，塌缩为一个判据）：

```
> **免确认旁路**：仅当用户原话**显式授权免确认**（明确要求不问就产出）时跳过本 CHECKPOINT；
> 原话含任何保留、延后或疑问语气时一律不跳过。跳过须在回复首行引用该原话。
```

若实测发现模型确实会误判，再叠加一条最小护栏（如「显式授权＝出现『不用问我 / 直接产出』这类短语，且该短语前 4 字内无否定词」），**不要预先铺满整套字符距离规则**。

### 🟡 P1-3 · 共享判定该放哪里：不要用跨目录 `shared/`

交叉代理提议抽到 `D:\project\skills\shared\免确认判定.md`。**主代理判断：不建议**。理由——skills.sh 按技能目录单独安装，`shared/` 不在任一技能目录内，单技能安装后指针即断链，这是用一份 SSOT 换一处静默断链，代价高于收益。

**建议方案**：把判定文本**复制进各自 `references/`**（`cognition/references/免确认判定.md`、`foundation/references/免确认判定.md`），两侧 SKILL.md 只留一行指针，正文不复述规则。制造受控 duplication 换取安装可靠性；日后若仓库确定只整仓发布，再合并为外部 reference。两份必须**同步修订**（P0-1 的漂移即由此前各自演化造成）。

### 🟢 已对齐、无需动的部分

异常表三列结构（触发 / 一线修复 / 仍失败兜底）、`🔴 CHECKPOINT · 🛑 STOP` 标记、加粗内嵌「完成判据」的写法、兜底原则「宁可留白不可编造」——两份一致，保持。留白标记形态不同（`cognition` 三型前缀 vs `foundation` 单一「待补充：」）属**领域必要差异**，不改。步骤命名「第一步」vs「操作一」是随手分叉，收益低，可选统一。

---

## 三、cognition 逐条

### 🔴 P0 · description 复述正文身份，且同一 branch 写两遍

L3 现状（节选）：「知识深度树：给定任意实体，产出九维逐层、从表层默认认知挖到深层真相的知识文档。展开维度交齐黑话/白话/案例三件套与有据的反转……当用户要求深挖某个实体，或要求构建/生成知识深度树文档时使用。」
**问题**：①「九维 / 三件套 / 反转 / 留白不编造」全是 L8、L12、L30、L126 正文已载的身份；②「深挖某个实体」与「构建/生成知识深度树文档」是同一 branch 的同义重写（判据：一 branch 一触发器）；③ 触发词被压在句末。
**建议文本**：

```yaml
description: 知识深度树：给定任意实体产出九维深度文档，以「反转」把默认认知翻过来。当用户要求深挖某个实体、或要生成知识深度树文档时使用。
```

### 🔴 P0 · 跳过条件块（见 P0-2，同一处）

### 🟡 P1 · L98 完成判据与模板实际形态不匹配

L98 要求「模板每个占位符已填」，但 `templates/knowledge-doc.md` 里 `补充（可选）`（L26–28）可空、`○ 简述维度`（L36）不复制三件套表 → 「每个占位符已填」无法分辨 done / not-done，正是判据点名的 premature completion 诱因。
**建议**：`完成判据：保留的占位符全部已填（○ 节点无三件套表、「补充」节可空，均不判为未填）；无遗留提示注释与示例文字；无模板写作说明残留；树序完整无断档。`

### 🟡 P1 · 「≥2 个独立来源」在 5 处复述

L74 / L90 / L114 / L120 / `domains.md` L48。**判据**：SSOT。保留 L74 为唯一定义，其余改为「来源规则见『异常与兜底』表」，`domains.md` L48 整句删除并留指针。

### 🟡 P1 · 「留白三类」定义两遍

L104（前缀 + 格式）与 L106（(a)(b)(c) 分类）重复。L106 只保留「案例位留白按其原因归入 (a) 或 (b)，不是独立一类」，分类定义留在 L104。

### 🟡 P1 · L126「不编造」整条可删

negation 驱动，且与 L122「宁可留白，不可编造」、L30「不得为凑数而造反转」同义重复。保留 L122 的正向句即可。

### 🟡 P1 · L55 领域数缺约束

L55「（十一个，可多标）」缺「最多三个」——该限制只存在于 `domains.md` L72，正文断链。补为「（可多标，最多三个）」。

### 🔵 P2 · 指针漏列子项 / 跨文件复述

- L48 指针称 `dimensions.md` 含「查什么、反转藏哪、黑话样本、好案例」4 项，实为 7 项（缺「要回答」等）→ 补齐或改为「逐维度填法」。
- L49 指针漏提 `domains.md` 的「多领域实体」条目。
- L34 与 L55 的 11 个领域名与 `domains.md` 表格重复 → 正文只留指针。
- `domains.md` L82–84「覆盖抽检」属论证性内容、永不触发 → relevance 检查不通过，可删。

### ✅ 做得好的地方

1. **L79 完成判据**：列四类取证（已采用 / 检索型留白 / 索取型留白 / 工具不可用留白）+「四者皆无即为未完成」，可数、穷尽、强制 legwork。
2. **L104 留白格式单点定义**：`<前缀>：<缺什么>｜去哪找：<补的办法>`，并显式规定附录扩展形态是唯一允许的变体、禁止另造「待补充」等变体。这是格式的唯一 SSOT，写得很干净。
3. **异常表把 legwork 写进表内**（「换检索式、换语言、查一手文件再试一次」），而不只是给兜底。

**实证**：内部互引均有落点——「豁免态②」=L90②、「异常表该行」=L114/L115、「跳过条件」=L63、「留白与检索记录」=模板 L56，**无悬空编号**；模板 `../references/` 相对路径正确。

---

## 四、foundation 逐条

### 🔴 P0 · 文件层判据缺阈值 → 会覆盖用户自有文档（实际风险）

**事实**：L14–16 文件层以「公共章节重合」为可写判据，而「公共章节＝除 H1 之外的标题与表头」。但 `templates/AGENTS.md` 的标题是 `## 项目介绍 / 场景上下文 / 技术架构 / 项目目录 / 项目关系 / 引入约定 / 开发命令 / 操作手册 / 编码开发`——**全是通用标题**。
**后果**：任何项目自带的 AGENTS.md 只要用了其中一两个通用标题，就判定为「部分重合 → 模板损坏」，走补齐路径**覆盖用户自有文件**，直接违反 L13「其余文档不创建、不修改」。`templates/TODO.md`（`## 一、公共待办`）、`DMS.md` 同理。
**判据**：完成判据 clarity + 硬护栏失守。
**建议**：文件层加最小重合阈值——`公共章节重合 ≥2 个特征标题（特征标题＝本技能特有的「## 场景上下文」「### 约束」这类），或命中任一特征标题`，否则判为非本技能产物、跳过。

### 🔴 P0 · 归一化对照口径寄居在「操作二」，却被公共节与「操作三」引用

**事实**：定义在 L89（操作二），引用方是 L28（「污染防护」，公共节）与 L110（操作三完成判据）。
**判据**：branching 测试——各分支都要用的规则却只写在其中一个分支里；初始化分支也被迫跨读操作二。属层级倒置。
**建议**：上移为「模板机制」下的独立 reference 段 `**对照口径**`（占位符→通配、删注释、`#` 层级一致），L28 / L89 / L110 三处改为同义引用。

### 🔴 P0 · 免确认词表嵌在「操作一」，却自称三操作通用

L75 自称「（三操作通用，唯一）」，但物理位置在「操作一」第 3 步之下，被 L68 / L83 / L99 三处引用。全流程唯一旁路闸门放错分支。
**建议**：提为顶层公共节 `## 免确认旁路`，三处 CHECKPOINT 只留「跳过条件见『免确认旁路』」。同时按 P0-1 补否决条件。

### 🟡 P1 · 「管辖范围」硬约束复述三遍

L13–17、L121–123（异常表两行）、L133。**判据**：SSOT。只留 L13–17，其余改为「按『管辖范围』节处理」。

### 🟡 P1 · 「读取优先级（唯一规则）」的例外被双写

L26 给出例外①②，L28 又二次展开同一组例外 → 「唯一」名不副实。L28 只保留比对方法（公共章节怎么比），例外留在 L26。

### 🟡 P1 · negation 过载且多数无正向目标

L13、L17、L23、L28、L108、L138 密集使用「不创建、不修改、不迁移、不删除、不写入」。判据允许保留无法正向表述的**硬护栏**，但须配正向目标。
**示例改写**（L17）：`写入面＝该目录下的四份同名模板；其余文件一律只读。`

### 🟡 P1 · 操作二完成判据埋在 bullet 里

L91 的完成判据夹在「更新规则」列表中，与 L73 / L110 的独立 `**完成判据**` 段格式不一致 → 格式不一致本身就是注意力漏检点。提为独立段落。

### 🟡 P1 · 同一判据两套命名

L16「重合」vs L28「缺失 / 对得上」。统一为「公共章节重合度」。

### 🔵 P2

- L140 的 `:13` 行号指针随编辑失效 → 改引标题「管辖范围」。
- L91「19 个挖空点」硬编码（当前实测 18 注释 + 1 占位，正确但脆）→ 改为格式示例。
- L136–140「存在性 + sha256 + 文件名清单」复述四处 → 在 L137 定义 leading word「**基线快照**」，后续只引该词。
- L136 对 `git status` 的三句论证压为一句：`不用 git status——被忽略路径漏报、未跟踪文件误报。`
- L3 description 建议：`项目四文档基建：初始化/更新/同步仓库根目录的 AGENTS.md、DESIGN.md、TODO.md、DMS.md，并把项目文档长出的结构反哺回 templates/ 模板。`

### ✅ 做得好的地方

1. **零改动判据 + 越界定义（L139–140）**：把「不越界」从一句断言变成哈希 + 文件名清单的可核验条件，且明示「如实上报不构成豁免」——这是全仓最强的一条判据。
2. **免确认词表封闭枚举 +「未命中即不免确认」**：消除歧义、防外扩。
3. **归一化口径 +「漏一个章节即未完成」**：可核验且 demanding，有效压制 premature completion。

**实证**：四形态挖空点在模板中均可落地（`<!-- 生成提示 -->` 多处、`{{项目名}}` 在 AGENTS L7 / DESIGN L1、TODO L11/L24 明示「属挖空点」、反哺标记按定义仅存于仓库根模板）；生成提示与「填空规则 5 条」一致，无冲突；L26/28/50/91/110/140 交叉引用全部可解析，**无悬空、无循环**；四处「唯一」互不冲突（仅 L26 例外被 L28 双写）；sha256 机制可执行，「动手前记录」与「范围固定」自洽。

---

## 五、仓库层面（非技能本体）

1. **README.md 仅 32 字节**（`# skills` + 一行），未列技能索引与安装方式 → 补索引。
2. **`skills-sh-发布指南.md` 与仓库现状不符**：指南称技能位于 `skills/knowledge/` 等分类桶下，实际是扁平的 `skills/cognition`、`skills/foundation`；指南提到的 `skills/foundation-workspace/` 已不存在。指南需同步。
3. **仓库根的 `darwin-*` 评测产物（1 份 md + 1 份 html + 1 份 png + 1 份 tsv）会随仓库一并发布** → 建议移出仓库或加入 `.gitignore`。
4. **router skill 不需要**：判据是「user-invoked 技能多到记不住」，本仓两个技能且均为 model-invoked，router 无对象可路由。

---

## 六、建议修改顺序

1. **foundation 文件层阈值**（P0，先堵覆盖用户文件的实际风险）
2. **统一免确认判定**：foundation 补否决条件 + 两份各自落到 `references/` + 正文只留指针（P0-1 / P0-2 / P0-词表位置）
3. **foundation 归一化口径上移**为「模板机制」下的公共 reference（P0）
4. **两份 description 改写**（P0，改动小、收益直接）
5. **SSOT 收敛**：≥2 独立来源（cognition）、管辖范围（foundation）、基线快照（foundation）
6. **negation → 正向**：cognition L126 删除、foundation L17 改写
7. P2 项与仓库层面清理

---

*本报告由三代理并行审阅 + 主代理实证复核产出；判据条款均出自 `writing-for-agents`。所有 P0 均附原文行号或片段，可逐条回查。*

---

## 七、修复状态（同日全量落地）

### 已改

| 项 | 处置 | 文件 |
|---|---|---|
| P0 免确认旁路两份实现 | 统一判定规则，各自副本落到 `skills/<name>/references/免确认判定.md`（不用跨目录 `shared/`，避免单技能安装断链）；正文只留指针 | 新建 2 份 + 两份 SKILL.md |
| P0 foundation 文件层阈值 | 新增「特征标题」表（四份文档各列本技能特有的标题），命中任一才写入；通用标题不作数 | `foundation/SKILL.md` |
| P0 cognition 伪解析器 | 5 行字符偏移规则 → 正向判据 + 三条硬护栏（保留／延后／疑问），外推至 reference | `cognition/references/免确认判定.md` |
| P0 cognition description | 去掉正文身份复述与同义 branch，触发词前置 | `cognition/SKILL.md` |
| P0 foundation 归一化口径 | 上移为「模板机制」下的公共 `**对照口径**`，三处改引用 | `foundation/SKILL.md` |
| P0 foundation 免确认词表位置 | 提为顶层公共节 `## 免确认旁路`（含 foundation 特有的一线修复处置口径），三处 CHECKPOINT 改引用 | `foundation/SKILL.md` |
| P1 来源口径 SSOT | 「≥2 独立来源」定为「第二步」唯一定义处，第三步判据与 `domains.md` 改引用 | `cognition/SKILL.md`、`domains.md` |
| P1 完成判据清晰度 | 「模板每个占位符已填」→ 明确 `○` 节点与「补充」节不判为未填 | `cognition/SKILL.md` |
| P1 领域上限断链 | 补「最多三个」，11 个领域名改为指向 `domains.md` | `cognition/SKILL.md` |
| P1 negation | cognition「不编造」整条删除并入「反转有据」；foundation「不创建、不修改、不删除」→「只读」 | 两份 SKILL.md |
| P1 管辖范围三处复述 | 异常表两行与执行纪律一条改为引用「管辖范围」 | `foundation/SKILL.md` |
| P1 操作二完成判据 | 提为独立段，并去掉硬编码「19 个挖空点」改为实计格式 | `foundation/SKILL.md` |
| P1 术语两套命名 | 统一为「公共章节重合度」，删除污染防护节与读取优先级的例外双写 | `foundation/SKILL.md` |
| P2 脆指针 / 硬编码 | `:13` → 引「管辖范围」标题 | `foundation/SKILL.md` |
| P2 leading word | 定义「基线快照」，核验与零改动判据改为引用该词；`git status` 三句论证压为一句 | `foundation/SKILL.md` |
| P2 指针漏列 | `dimensions.md` 指针补为七项，`domains.md` 指针补「判定次序 / 多领域实体」 | `cognition/SKILL.md` |
| P2 relevance | 删除 `domains.md`「覆盖抽检」（论证性内容、永不触发） | `domains.md` |
| 仓库层 | README 补技能索引与安装方式；发布指南的 `skills/knowledge/` 桶与 `foundation-workspace/` 两处过时描述改为扁平布局与当前产物；7 个 `darwin-*` 评测产物移入 `eval-产物/`（移动，非删除） | `README.md`、`skills-sh-发布指南.md` |

### 未改（判定后保留）

- **留白三类（cognition L104 前缀格式 + L106 分类判据）**：复核后认为两者互补而非重复——L104 管**格式**，L106 管**成因与检索式要求**，缺一不可执行。仅收敛了结尾措辞，未做删并。
- **步骤命名「第一步」vs「操作一」**：纯风格分叉，统一收益低于改动成本（会牵动全部交叉引用）。

### 校验结果

- 全仓 Markdown 相对链接**断链 0**；
- 两份 frontmatter `name` / `description` 完整；
- `免确认词表`／`≥2 个独立来源`／`不创建、不修改、不删除`／`:13` 四处残留关键词**清零**；
- 体积：cognition 15.6 KB → 13.7 KB（−12%）；foundation 18.1 KB → 18.4 KB（+0.3 KB，新增的是 P0 特征标题表，同时把 350 字的免确认词表移出常驻上下文）。
