# {{项目名}} 产品说明

> 本文档回答「产品由哪些应用/模块组成、模块间如何关联与引用、业务按什么顺序流转、动一个实体会牵连什么」，供开发前定位改动影响面使用。
>
> <!-- 生成提示：注明应用/模块清单的事实源（应用注册表、服务端菜单、路由配置）与动态路由机制一句话；无应用维度的单体项目改写为模块清单来源。 -->
>
> 状态取值：在建｜稳定｜存量｜空壳｜停用；未核实的位置/状态一律标「待补充」，不臆断。

<!-- 生成提示：按业务分组（云分类/业务线/领域）分一级章节，组内每个应用/模块一个二级小节，按下方骨架重复填写。应用 = 标题 + 要点列表，不用表格；「增/删/改/查关联」「特殊字段」是应用下的子标题。分组与应用命名对齐事实源，勿自创。 -->

## {{分组名（云分类/业务线）}}

### {{应用名（标识/appKey）}}

- 位置：<!-- 生成提示：页面组件所在目录；不确定时标「待补充」并注明核对线索（如后端菜单 id、design-doc 文档名）。 -->
- 职责：<!-- 生成提示：一句话。 -->
- 状态：<!-- 生成提示：按状态取值填；依据不足标待补充。 -->

模块 → 子模块：

<!-- 生成提示：按目录事实列出「**模块**：子模块、子模块…」清单；单页模块注明无下级。空壳/停用应用可省略本项及以下子标题。 -->

#### 增/删/改/查关联

<!-- 生成提示：对该应用的核心实体逐条说明：增（来源/前置，如经选择弹窗带入、上游单据带出）、删（引用校验；服务端行为前端不可见时一律标待补充）、改（联动/拦截，如变更管控、状态回写）、查（关联查询/回显契约）。只写有实现依据的。 -->

#### 特殊字段

<!-- 生成提示：该应用特有的字段约定（成对冗余字段、查询 Vo[]/保存 id[] 契约、类型+id+范围三件套等）；没有则写一行「见平台层全局通用约定」。 -->

## 平台层（不属于任何应用的公共能力）

<!-- 生成提示：门户/工作台等平台页面清单；随后两个子标题：「特殊字段（全局通用约定）」——附件主键、状态布尔风格、命名空间键、关联下拉外键规则等；「增/删/改/查通用规则」——导入导出接线、树形数据契约、删除确认与服务端校验的待补充口径。 -->

## 模块关联

<!-- 生成提示：逐条列出跨应用/跨模块的关联机制：应用与路由的关系（动态菜单/命名空间）、单向依赖契约（谁挂载谁、允许方向）、公共组件层与全局弹窗注册表的被引用关系、子模块隔离规则、api/types 目录对应关系。禁止依赖方向只列有代码依据的，其余以「待补充」收尾单列一条。 -->

## 业务顺序

<!-- 生成提示：主线按序号列步骤，每步标注承载应用/模块与关键单据/实体；支线（如委外、设备保障、审批）单独一段；末尾配一张 mermaid flowchart 与文字结论一致。 -->

## 应用关联图

<!-- 生成提示：mermaid flowchart，按分组 subgraph 画出全部应用与关键数据流向（实线=数据流，虚线=支撑关系），与「模块关联」「业务顺序」保持一致；应用多时只画有连线的。 -->
