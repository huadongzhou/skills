---
alwaysApply: true
---

<!-- 生成提示：上方 frontmatter 仅在团队使用会消费它的工具（如 Cursor rules）时保留，否则连同分隔线一起删除。 -->

# {{项目名}}

<!-- 生成提示：标题用仓库实际名称。本文档是默认上下文，追求最简单清晰的指引，总长控制在 200 行内；写不满不硬凑。 -->

## 项目介绍

<!-- 生成提示：一句话定位（形如「X 基座 + N 拓展应用」或「单体应用：解决什么问题」），随后 2-4 个要点列出核心域/模块及其源码位置。来源：README、package.json、src 顶层目录。 -->

## 场景上下文

以下文档不会自动进入上下文，命中引入时机先读再动手；按章节读所需部分，不必全文。

- **PRODUCT.md** —— 产品说明：应用/模块组成与所在目录、业务流转顺序、实体增删改查的跨模块牵连。
  - 引入时机：接到业务需求定位改动范围时；新建或改造业务页面前；改实体/接口需评估上下游影响时；不确定某功能归属哪个应用/模块时。
- **DESIGN.md** —— UI 设计系统：设计原则、Design Tokens（品牌色/中性色/暗色模式/字体）、组件视觉规范；样式编码所用设计令牌以此文档为唯一事实源。
  - 引入时机：写或改任何视觉样式（颜色、边框、圆角、字体、阴影、暗色适配）前；新建页面搭布局前；评审 UI 还原度时。禁止凭记忆写色值，先查此文档。

<!-- 生成提示：五文档体系的按需引入接线，上方两条为通用基线；条目描述与引入时机可按项目微调，项目另有场景文档（操作手册、领域词表等）时续列，格式一致。 -->

## 技术架构

<!-- 生成提示：从 package.json 依赖与构建配置提取，按条列出：语言 / 框架 / 构建 / 路由 / 状态 / UI / 样式 / 网络 / 国际化 / 质量工具 / 工程方向。只列实际在用的，不罗列全部依赖；存量兼容项标注（存量）。 -->

## 项目目录

<!-- 生成提示：源码一级目录树（```text 代码块），每行尾注一句职责说明；存量/待废弃目录明确标注，避免新代码写进去。 -->

## 项目关系

<!-- 生成提示：mermaid flowchart，表达 入口 → 主体 → 子模块 与公共层（组件/工具/路由/状态/插件等）之间的依赖方向；有代码依据时标注禁止的依赖方向（如子模块互相依赖），无依据勿推定。单模块项目可简化为分层图。 -->

## 引入约定

<!-- 生成提示：路径别名清单、依赖方向规则（谁可引用谁、禁止方向）、共享能力的下沉规则。 -->

## 开发命令

<!-- 生成提示：dev / build / lint / format / test 命令与注意事项（端口、耗时、易 OOM 等），Node/运行时版本要求，环境变量文件位置。以 package.json/构建配置的声明为准列出，不要求实测；已知跑不通的注明原因，未接入的（依赖存在但无脚本）不写。 -->

## 操作手册

<!-- 生成提示：项目有专项操作手册（如浏览器/终端驱动走查说明、部署手册、联调手册）时，逐条列出手册用途与文档路径（如 docs/xxx-om.md），指引代理按手册操作；没有则删除本节。 -->

## 编码开发

### 指南

倾向谨慎而非速度。

- **先想后写**：有歧义先问、别私下选；有更简方案先讲；假设与困惑都要摆明。
- **简约至上**：解决问题的最小代码；不做没要求的「灵活性/可配置性」；一次性代码不抽象；不为不可能的分支写兜底。
- **手术式改动**：只碰必要处、与现有风格一致；不顺手重构或改无关代码/格式；改动产生的孤立 import/变量要清掉，既有死代码只标记不删除。
- **可验证收尾**：把任务转成可检查的成功标准并循环至通过。<!-- 生成提示：补一句本项目实际可用的验证手段（哪些 test/lint/typecheck 能跑、哪些跑不了以及替代方案）。 -->

### 约束

- 以需求和仓库现状为准，信息不完整时可做合理假设并注明。
- 涉及改动前先确认影响范围，尽量减少对无关模块的影响。
- 注意嵌入代码位置上下文逻辑是否有影响。
- 不要删除代码注释，保持原有代码可读性。
- 输出格式要保持原有的编码格式（中/英文）。
- 禁止引导式噪音内容。

### 语言

<!-- 生成提示：按项目技术栈/语言分小节（前端如 vue / html / css / js / ts，后端按语言/框架），每小节 3-6 条本项目强制的编码约定（组件写法、代码分区顺序、类型组织、设计令牌使用、错误处理等）。从现有代码归纳真实惯例，而非泛泛的最佳实践。 -->

## agent专用目录

`agent/` 用于承载代码助手生成的临时文件、方案文档与任务执行记录，避免污染业务源码目录。
建议分层：`pages`（需求页面文档）、`task`（任务执行表）、`docs`（过程文档）、`plan`（方案文档）、`temp`（临时文件/日志）。
所有不进入源码目录的产物（计划、调研、临时脚本）一律放 `agent/<分类>/`，禁止在仓库根目录或源码目录下创建 *.md 临时文档。
