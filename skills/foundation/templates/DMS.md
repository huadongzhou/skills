# DMS · 文档管理规范

> 管理范围：<!-- 生成提示：列出本规范管辖的非代码目录（如 resources/ prd/ plan/ task/ docs/ temp/），逐个用反引号括起。 -->
> <!-- 生成提示：写明代码所在位置，并声明「本规范内的文档一律留在主仓，不在代码目录内新建」；单代码根、无子模块的项目删去后半句。 -->

---

## 1. 总览

<!-- 生成提示：逐目录一行、六列。定位一句话说清收什么；典型内容列 2-3 个代表物；组织方式写路径模板（如 {app}/{模块}/{page}/、YYMMDD-主题/）；生命周期写保留 / 更新 / 清理策略；入库列标 ✅ / ❌。目录集合按项目实际增删，须与 §3 各小节一一对应。 -->

| 目录 | 一句话定位 | 典型内容 | 组织方式 | 生命周期 | 入库 |
| --- | --- | --- | --- | --- | --- |

### 1.1 流转关系

<!-- 生成提示：一张 text 流程图，串起「外部输入 → 需求 / 方案 → 执行跟踪 → 代码」主链，标出长期结论沉淀到哪个目录、哪个目录贯穿全程且不被任何文档引用。用项目实际目录名替换。 -->

### 1.2 放哪儿？速查

<!-- 生成提示：「我手上的东西是… → 放到」两列表，覆盖 §1 总览每个目录的典型输入；有例外落点（如随代码走的 SQL、脚本）在括号内注明并指向 §3 对应小节。 -->

| 我手上的东西是… | 放到 |
| --- | --- |

---

## 2. 通用约定

<!-- 生成提示：逐条给跨目录的命名与卫生结论。常见基线：日期前缀取创建日、之后改名不随修改；目录 / 文件名允许中文，路径段 kebab-case（与代码目录保持一致）；文本一律 UTF-8、优先 Markdown；不复制 -v2 / -最终版 / -副本 做版本管理，历史交给 git（例外目录见 §3）；跨目录引用写相对仓库根的路径；一事一处，同一内容只放一处、别处写引用。保留适用的、删掉不适用的，项目特有约定续列。 -->

---

## 3. 各目录说明与管理方式

<!-- 生成提示：按 §1 总览的目录逐一开小节，每节统一三段：**描述**（收什么、在流程中处于哪个位置）、**结构**（text 目录树 + 命名示例）、**管理方式**（表格或列表，覆盖 新增 / 命名 / 更新 / 状态标记 / 完成 / 废弃 / 例外）。只写项目实际存在的目录，无内容的整节删除。 -->
