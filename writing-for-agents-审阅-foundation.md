# foundation 技能审阅报告（writing-for-agents 框架）

审阅对象：`skills/foundation/`（SKILL.md + templates/ 四份）
框架依据：`writing-for-agents/SKILL.md`、`SKILL-MECHANICS.md`

---

## 1. 一句话结论

最要紧的问题是**「模板」一词在指针层与正文层都没有消歧**：L19-20 明确存在两个 `templates/`（技能自带的只读基准、仓库内的写入目标），但 L81、L101、L105、L117 一律裸写「模板」，同一裸词在同一文档里指两个文件；叠加 L83 那条可被「无变化」一键满足的完成判据，「更新」最可能用错基准、并把没做的事汇报成做完。

检查面结论：a 有问题｜b 通过｜c 有问题｜d 有问题｜e 通过（一条需澄清）｜f 部分有问题｜g 有问题。

---

## 2. 关键发现

### 高 1｜「模板」指针歧义，且后果具体

位置：L19-20、L22、L81、L101、L105、L117。

L19 说技能自带模板是「只读基准……初始化与更新都读它」，L20 说仓库内模板是「同步的写入目标」，但下游全部退化为裸词：L81（更新）按 L19 应指基准模板，L101/L105（同步）按 L91 应指仓库模板——同一词两个指称。后果：某章节若已被「同步」反哺进仓库模板而基准模板尚无，L81 的「保留（文档有而模板没有的章节）」会把它误判为同步输入，每次更新重复上报同一个待反哺候选。

依据：Context pointers（「The pointer's wording, not its target, decides when the agent reaches the material」）；Pruning 的 single source of truth。

改法：L19/L20 表格首列立术语——「技能自带 `templates/<名>.md`（下称**基准模板**）」「仓库内 `templates/<名>.md`（下称**仓库模板**）」；随后替换：L81→「对照**基准模板**逐章节过一遍」；L101→「在**仓库模板**中找不到同类兄弟节点」；L105→「每个**仓库模板**没有的标题与表头」；L117→「不写入**仓库模板**」。并在 L81 后补：「章节若已存在于仓库模板，不计入『保留』，判为『无变化』。」

### 高 2｜「更新」的完成判据不可检查、无需求

位置：L83。原文「**完成判据**：所处理文档的全部章节处置有交代，缺一份即未完成。」

三处硬伤：①「所处理文档」未定域（四份？还是 L124 点名的那几份？）；②「有交代」是决定过还是汇报出来，不可观察；③计量单位自相矛盾（前半数「章节」，后半数「份」）。更严重的是 demand：L81 的四选一含「无变化」而它无举证义务，模型可把每节一律标「无变化」而判据全绿。

依据：Steps and completion criteria（Clarity 防 premature completion；Demand 驱动 legwork）。改法：整段替换为——

> **完成判据**：本次点名的每份文档，其每个章节都有一条处置（补齐／修正／保留／无变化），并在汇报中按章节列出处置与依据；标「无变化」须写出与基准模板的逐条对照结果（如「模板 3 个挖空点均已填」），仅凭印象标注不算。漏一个章节即未完成。

### 高 3｜description 把三个分支写了两遍，结尾句不是分支

位置：L3。原文把「初始化、更新、同步」先作操作名列一次，再作释义句（搭建／跟上现状／反哺）列第二次——一个分支写两遍，而 description 是唯一常驻上下文，每轮都付费。结尾「即使用户没有说出技能名或完整文档名」不改变任何路径（分支的定义是「different runs take different paths」），只是复述 model-invocation 的既有语义（SKILL-MECHANICS.md:9「model-invocation always includes user reach」），按 no-op 判据应整句删除。依据：Context pointers、Pruning 的 no-op。改法：整行替换为——

> description: 项目根目录四文档基建：AGENTS.md、DESIGN.md、TODO.md、DMS.md。用户要为项目搭建缺失的文档（初始化）、要让既有文档跟上项目现状（更新）、或要把项目文档长出的结构反哺回模板（同步）时使用。

### 高 4｜三份参考压在三个操作之前，把步骤埋了

位置：L10-47 全部位于首个操作 L49 之前。其中 L41-47「填空规则」只在填空时查阅（同步用的是它的反操作，见 L39）。按信息层级，steps 是第一梯队、in-file reference 是第二梯队；38 行参考压在操作章之上，框架警告这会让「attending to them 变成 coin-flip: a variance lever」。改法：保留「管辖范围」在最前（它是闸门），把 L24-33「文档定位」与 L35-47「模板机制」整块移到「操作三」之后、「异常与兜底」之前；填空规则被 2/3 分支使用，留在文件内，只调顺序。依据：Information hierarchy / Progressive disclosure、Sprawl。

### 中 1｜「不碰其他文档」这条禁令写了三处

位置：L13、L116、L124。L13 已完整声明并列出文档清单，L116 异常表再列一遍，L124 又指一次。且 L13 是三个「不」的否定式堆叠——框架指出禁止式写法会把被禁行为拉进上下文、反而提高其可用性；对照 L103「方向单一：项目文档 → 模板；同步过程永不反向删改项目文档」，先正面后禁令才是正确范式。改法：L13→「本技能管辖的只有仓库根目录四份文档；`README.md`、`CLAUDE.md`、`docs/` 等其他文档只在『解读项目』时作为只读来源读取。」；L116→「用户点名管辖范围外的文档｜按『管辖范围』节说明只管辖四份文档，不落笔｜用户坚持时记为『超出管辖范围』，不写入待办」；L124 删去重复清单，改为「点名范围外的文档按『异常与兜底』表处理」。

### 中 2｜「文档定位」表缓存了模板首段

位置：L26-31 的「是什么」列；L33 自认「每份文档的完整定位与结构见其模板首段」。既然模板首段是定位的事实源（如 `templates/AGENTS.md` L15-26 的场景上下文），该列即一份 cache；而「解读项目时的主要来源」列是模板没有的未成文约定，应留下。改法：删「是什么」列，表头改为「文档 ｜ 解读项目时的主要来源」，保留 L33 指针句。依据：Pruning 的 cache。

### 中 3｜授权旁路写了两遍，且触发条件模糊

位置：L63「用户已在本次请求中明确要求直接生成时跳过」与 L98「用户已明确要求直接同步时跳过」。同一含义两处，且「明确要求直接生成」不可判定——「帮我初始化这四份文档」算不算？它是 checkpoint 能否生效的唯一开关。改法：在「执行纪律」新增单一事实源：「**授权旁路**：仅当用户在同一请求中出现『直接生成／直接写／不用确认』时跳过 CHECKPOINT；『帮我初始化这四份文档』不构成旁路。」L63/L98 改为「（授权旁路见『执行纪律』）」。

### 中 4｜三个 CHECKPOINT 不是上下文边界

位置：L63、L98、L102。框架对「靠隐藏后续步骤防早停」设了硬条件：只在真实上下文边界（hand-off 或子 agent 派发）有效，内联停等「leaves the later steps in context and clears nothing」。三处都是内联停等，不提供防早停收益，但「🔴 CHECKPOINT · 🛑 STOP」的措辞易被读成边界。改法：不改行为，在「执行纪律」加：「CHECKPOINT 是用户确认闸门，不是上下文切分；防早停靠完成判据的清晰度，不靠 STOP。」

### 中 5｜模板「约束」节是泛泛最佳实践

位置：`templates/AGENTS.md` L65-70，对照同文件 L74（生成提示要求「从现有代码归纳真实惯例，而非泛泛的最佳实践」）。L65-70 预置「以需求和仓库现状为准」「禁止引导式噪音内容」等与项目无关的通则且不留挖空点，模型照抄即产出 no-op；末句更是无正面对照物的禁令。改法：整块换成「<!-- 生成提示：逐条列出本项目**特有**的硬约束（从现有代码、配置、历史事故归纳）。通用工程通则不写。 -->」。

### 低 1｜exposition 与自造词

①L8「为项目准备稳定的根级上下文文档……即拥有一致的认知基础」不改变任何分支行为，身份已由 L3 与 L6 承担（mere exposition），删除或压成标题下一句短语。②L37/L44/L46/L101 用自造词「挖空点」，L39 却已立出「完形填空」与「填空／挖空」对偶——后者是预训练强概念（免费），前者要付定义成本，二者择一全篇只留一个 token。

### 低 2｜整句比划可收敛为领先词

L81「每份文档对照模板逐章节过一遍，每个章节给出四选一处置」可改为「逐章节**对账**基准模板，每节给一个处置：补齐／修正／保留／无变化」——「对账」承载「逐项核对并给处置」的既有语义。另 L45 与 `templates/AGENTS.md` L38 各说一遍「禁止依赖方向」这条具体规则，模板 L38 保留，L45 删去该举例、只留通则。

---

## 3. 通过项

- **b) 两级负载**：templates/ 披露位置正确。四份模板被三个操作全部读取（L61/L81/L91），属「所有分支都要读」，不该下沉也不该内联；description 是唯一常驻负载项，分层没错，错的是 L3 措辞。
- **d) 初始化判据可判定**：L67 枚举单位明确（四份）、有可观察量（字节未变）。
- **d) 同步判据是全篇最佳**：L105「每个模板没有的标题与表头，都有一条处置……漏一个即未完成；项目文档本身零改动」既 checkable 又 exhaustive，是 L83 应效法的样板。
- **e) 不应拆分**：三操作互斥非序列（L123「一次调用只做一种操作」），sequence cut 不适用；三者共享模板机制、异常表、执行纪律，拆开要么三份重复、要么需第四份共享参考；按调用方式也拆不出差异。
- **e) model-invoked 正确**：触发来自自然语言意图，用户不会说出技能名，符合 SKILL-MECHANICS.md:12「the agent must reach the skill on its own」；成本问题靠修剪解决，不是取消 description。
- **e) CHECKPOINT 不是 no-op**：模型默认直接落盘，闸门确实改变行为。
- **f)「反哺」是好领先词**：L3/L85/L92/L100/L103/L105 一致复用同一 token；L103 先正面目标再配硬禁令，是 negation 的正确用法。
- **g) L122、L109、L123 都成立**：分别对抗「复用旧读取内容」「静默跳过」两个默认倾向，并把「该用哪个操作」的索引负担从人身上拿掉。

---

## 4. 不建议改的

- **gherkin 的 Given/When 保留**：它把触发条件与操作名绑在一起，是 description 分支在正文里的落点。但 `Then`/`And` 与编号步骤、完成判据大面积重合（L55≈L61-64，L56≈L67，L92-93≈L101-102），应把 `Then` 收敛为只留完成保证——L93「模板既有内容不被项目特性值污染」只此一处，应上移进 L105 的判据，其余删除。
- **异常与兜底表保留**：L111-118 六行都是「默认行为会静默跳过」的对抗项，不是 no-op。
- **「管辖范围」位置保留**：措辞需改（中 1），但护栏本身有价值——模型默认倾向是「顺手把 README 也整理一下」，必须挡住；不要随高 4 一起下沉。
- **「执行纪律」留在文末**：它是 reference，位于三个步骤之后，正是信息层级期望的顺序。
- **不要为「一致性」把 L83 与 L105 拉平**：L105 句式更强，正确做法是把 L83 改写到 L105 的强度，而非反向拉平。
